package com.eventmanagement.security;

import org.springframework.stereotype.Component;

@Component
public class JwtTokenProvider {

    // TODO: generateToken(), validateToken(), getUsernameFromToken()
}